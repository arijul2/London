# Premier League Ticket Monitor Configuration

# Match to monitor (format: "Home Team vs Away Team")
MATCH_NAME=Arsenal vs Everton

# Price range (in GBP)
MIN_PRICE=200
MAX_PRICE=500

# Number of tickets needed
QUANTITY_NEEDED=2

# Preferred sections/stands (comma-separated, optional)
# Leave empty to accept any section
PREFERRED_SECTIONS=North Stand,East Stand

# Check interval in minutes
CHECK_INTERVAL_MINUTES=30

# Email notifications
EMAIL_ENABLED=false
EMAIL_TO=your-email@example.com
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Pushover notifications (get API key from https://pushover.net)
PUSHOVER_ENABLED=false
PUSHOVER_API_KEY=your-api-key
PUSHOVER_USER_KEY=your-user-key

# Desktop notifications (macOS/Linux)
DESKTOP_NOTIFICATIONS_ENABLED=true

# Run once instead of continuously (for testing)
RUN_ONCE=false

# Fanpass base URL (usually don't need to change this)
FANPASS_BASE_URL=https://fanpass.net
